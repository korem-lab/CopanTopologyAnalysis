# config.yaml

# Manitou Directories
graphDir: "/manitou/pmg/users/rc3710/CopanTopologyAnalysis/config/copangraphs"
linksDir: "/link_dicts"
walkDictsDir: "/walk_dicts_oriented"
walkListsDir: "/walk_lists_vectorized"
modelDir: "/vectorization_model/models"
embeddingsDir: "/vectorization_model/embeddings"
edgeEmbeddingsDir: "/vectorization_model/edge_embeddings"
plotsDir: "/plots"
clustersDir: "/cluster_dicts"
distancesDir: "/pairwise_distances"
distancesDir: "/pairwise_distances"
degreeDir: "/node_degrees"
distDegDir: "/distance_degree"

# temp dir in pmg local and output dir in manitou
tempDir: "/pmglocal/rc3710"
outputDir: "/manitou/pmg/users/rc3710/CopanTopologyAnalysis/workflow/out"

# Graph ID
graph_id: ["copan_0"]

# Walk params
walk_lengths: [30, 80]  # list of walk lengths
n_walks: [10, 50]      # list of number of walks to perform per node
# p: [1.0, 0.5, 2.0, 0.1]             # list of p values
# q: [1.0, 0.5, 2.0, 0.1]             # list of q values
p: [1.0]             # list of p values
q: [1.0]             # list of q values
seed: 1

# Embedding params
dimensions: [20, 60, 100, 140]
window: 5
min_count: 5
sg: 0

# t-SNE params
perplexities: [10, 30, 50, 100]  # list of perplexity values
n_iters: [1000, 3000, 5000]     # list of n_iter values
n_components: 2
random_state: 32
